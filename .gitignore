.venv/
__pycache__/
.huggingface/

# Downloaded pretrained weights (~6.5GB total) -- see playground/handover doc for the exact
# huggingface-cli commands to re-fetch these; not meant to live in git history.
checkpoint/
ip_adapter_hf/
vae_hf/
base_hf/
/outputs/

# Local verification input data (real KDW photos, MVTec crops) -- not meant to live in git
# history; re-fetch/re-generate per the scripts under run_infer_*.py.
/assets/
